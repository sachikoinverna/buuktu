<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Color de fondo -->
            <corners android:radius="40dp" /> <!-- Radios redondeados -->
        </shape>
    </item>

    <!-- Borde -->
    <item android:left="2dp" android:top="2dp" android:right="2dp" android:bottom="2dp">
        <shape android:shape="rectangle">
            <stroke android:width="4dp" android:color="@color/redError" /> <!-- Color y grosor del borde -->
            <corners android:radius="40dp" /> <!-- Radios redondeados (debe coincidir con el fondo) -->
        </shape>
    </item>
</layer-list>
